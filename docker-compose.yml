version: '3.8'
services:
  refusalguard:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/backend/data  # 持久化知识库和日志
    environment:
      - HF_ENDPOINT=https://hf-mirror.com  # 国内镜像